apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: operador-contas
  namespace: tipsbank-contas
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log"] #tem que colocar pods/log so pod não leu logs
    verbs: ["get", "watch", "list"]
